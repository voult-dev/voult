## List of problems/bugs found in the current state of the app.

### WEB

- Cors is preventing successful login (FIXED)
- CSRF is preventing the creation of a new app in the web. (FIXED)


### API
- `Invalid CSRF Token` message test app login and signup
- Password validation is too strict
- Sign out sends out signout message even when their is no signed in user.