<% layout('layout/boilerplate') %>

<section class="container app-container">

  <div class="app-header">
    <h1>Create a New App</h1>
    <p class="text-white opacity-50">
      Create an app to start authenticating users with voult.dev.
    </p>
  </div>

  <form action="/app" method="POST" class="card app-form">
  <input type="hidden" name="_csrf" value="<%=csrfToken %>">
    <div class="form-group">
      <label for="name" class="text-white">App Name</label>
      <input
        type="text"
        id="name"
        name="name"
        required
        placeholder="e.g. My SaaS App"
      />
      <small class="form-help">
        This name is only visible to you.
      </small>
    </div>

    <div class="form-group">
      <label for="description" class="text-white">Description (optional)</label>
      <textarea
        id="description"
        name="description"
        rows="3"
        placeholder="What does this app do?"
      ></textarea>
    </div>

    <div class="form-group">
      <label for="callbackUrl" class="text-white">Callback URL (optional)</label>
      <input
        type="url"
        id="callbackUrl"
        name="callbackUrl"
        placeholder="https://yourapp.com/auth/callback"
      />
      <small class="form-help">
        Used later for OAuth & redirects.
      </small>
    </div>

    <div class="form-actions">
      <button type="submit" class="btn-primary">
        Create App
      </button>

      <a href="/dashboard" class="btn-secondary">
        Cancel
      </a>
    </div>

  </form>

</section>
